# --- Declarative intent (human-facing) ---
promotion_environment = "dev"
promotion_approved_by = "Change Advisory Board"
promotion_reason      = "No-Op"

# --- Executable intent (machine-enforced) ---
approved_refs = {
  network  = "v1.0.2"
  security = "v1.0.0"
}

